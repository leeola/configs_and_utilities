#!/bin/bash

if [[ `git symbolic-ref HEAD` == "refs/heads/master" ]]; then
    echo "You cannot commit in master!"
    exit 1
elif [[ `git symbolic-ref HEAD` == "refs/heads/develop" ]]; then
    echo "You cannot commit in develop!"
    exit 1
fi
